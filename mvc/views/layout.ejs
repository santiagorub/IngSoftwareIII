<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
    <style>
      body { font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; margin: 24px; }
      a { color: #2563eb; text-decoration: none; }
      a:hover { text-decoration: underline; }
      .btn { display: inline-block; background: #2563eb; color: #fff; padding: 8px 12px; border-radius: 6px; }
      .btn-secondary { background: #6b7280; }
      .card { border: 1px solid #e5e7eb; border-radius: 8px; padding: 16px; margin-bottom: 16px; }
      .error { color: #b91c1c; }
      label { display: block; margin: 8px 0 4px; font-weight: 600; }
      input { padding: 8px; width: 100%; border: 1px solid #e5e7eb; border-radius: 6px; }
    </style>
  </head>
  <body>
    <div class="card">
      <h1>MVC Puro - Books</h1>
      <div><a href="/books">Home</a> · <a href="/books/new">New</a></div>
    </div>
    <%- body %>
  </body>
</html>
